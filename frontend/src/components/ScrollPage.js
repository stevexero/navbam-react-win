import styled from 'styled-components';

const ScrollPageSection = styled.div`
  width: 100vw;
  height: 800px;
  background: #777;
`;

const ScrollPage = () => {
  return <ScrollPageSection>I'm the scroll page</ScrollPageSection>;
};

export default ScrollPage;
